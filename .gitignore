__pycache__/
*.pyc

# go build artefacts — 9MB binaries that also defeat text scanning
/reference/broker/broker
/reference/broker/switch-portal
preview/
docs/diagrams/*.png
!docs/diagrams/d1-deployment-diagrams.png
# Icon PNGs are rendered from the committed SVG sources by the command in
# README.md, so the published page needs no SVG-capable diagram build step.
!docs/diagrams/icons/*.png
